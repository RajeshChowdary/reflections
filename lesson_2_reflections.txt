Q1) What happens when you initialize a repository? Why do you need to do it?
  
  A) intialized empty git repository in the home directory and .git directory is created and it is not show in the users.
  
Q2) How is the staging area different from the working directory and the repository? What value do you think it offers??

    A) Git uses the intemediate area called the staging area. In the staging area you can git init, add , status before commits. After that you commit that adds that to the repository.

 Q3) How can you use the staging area to make sure you have one commit per logical change?

     A) Based on the logics,working time/hours we use the staging area.

 Q3) What are some situations when branches would be helpful in keeping your history organized? How would branches help?

     A) suppose when we created the different versions of the same version its helful. bacause it not effect to the original file
 
 Q4) How do the diagrams help you visualize the branch structure?
  
     A) it helps to easily understand the branches in the repository.
 
 Q5) What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

     A) After merging two branches, it will combines the two brances into one branch. I easily inderstand the branches.
     